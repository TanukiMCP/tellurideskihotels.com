---
export const prerender = false;

const { hotelId } = Astro.params;
const queryString = Astro.url.searchParams.toString();

// Redirect /lodging/[hotelId] to /places-to-stay/[hotelId]
return Astro.redirect(`/places-to-stay/${hotelId}` + (queryString ? `?${queryString}` : ''), 301);
---
