---
export const prerender = false;

// Redirect all /lodging routes to /places-to-stay
const queryString = Astro.url.searchParams.toString();
return Astro.redirect(Astro.url.pathname.replace('/lodging', '/places-to-stay') + (queryString ? `?${queryString}` : ''), 301);
---
