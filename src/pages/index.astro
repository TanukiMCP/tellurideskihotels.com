---
export const prerender = false;

import PageLayout from '@/components/layout/PageLayout.astro';
import Container from '@/components/layout/Container.astro';
import SplitHero from '@/components/lodging/SplitHero';
import { FeaturedHotels } from '@/components/lodging/FeaturedHotels';
import { CurrentConditions } from '@/components/weather/CurrentConditions';
import { FeaturedActivities } from '@/components/activities/FeaturedActivities';
import { SkiConditionsWidget } from '@/components/ski/SkiConditionsWidget';
import { GondolaWidget } from '@/components/info/GondolaWidget';
import { EventsWidget } from '@/components/info/EventsWidget';
import { AirQualityWidget } from '@/components/info/AirQualityWidget';
import { WebcamLinks } from '@/components/info/WebcamLinks';
import { SITE_DESCRIPTION } from '@/lib/constants';
import { getHomepageHeroImage, getImagesByCategory } from '@/lib/image-library';
import { optimizePexelsImage, generateSrcSet } from '@/lib/image-optimizer';
import { searchHotels } from '@/lib/liteapi/hotels';
import { getTellurideSkiConditions } from '@/lib/snocountry/client';
import { getTopHotels } from '@/lib/top-hotels';
import { getCollection, type CollectionEntry } from 'astro:content';
import FeaturedBlogSection from '@/components/blog/FeaturedBlogSection.astro';

const heroImage = getHomepageHeroImage();
const heroImages = getImagesByCategory('hero') || getImagesByCategory('resort') || [];
const ctaImage = getImagesByCategory('resort')[3] || getImagesByCategory('luxury')[0] || null;

// Fetch featured hotels server-side (has access to API key)
import type { LiteAPIHotel } from '@/lib/liteapi/types';
import { format, addDays } from 'date-fns';
import { getMinRates } from '@/lib/liteapi/rates';

let featuredHotelsData: LiteAPIHotel[] = [];
let topHotelsData: LiteAPIHotel[] = [];
let allHotels: LiteAPIHotel[] = [];
let minPrices: Record<string, number> = {};
let featuredCheckInDate: string | undefined;
let featuredCheckOutDate: string | undefined;
let skiConditions = null;

// Fetch featured blog article
let featuredBlogArticle: CollectionEntry<'blog'> | null = null;
let recentBlogArticles: CollectionEntry<'blog'>[] = [];
try {
  const allBlogArticles = await getCollection('blog', ({ data }) => {
    return data.status === 'published';
  });
  
  const sortedArticles = allBlogArticles.sort((a, b) => {
    return b.data.publishDate.getTime() - a.data.publishDate.getTime();
  });
  
  featuredBlogArticle = sortedArticles.find(article => article.data.featured) || sortedArticles[0] || null;
  recentBlogArticles = sortedArticles
    .filter(article => article.slug !== featuredBlogArticle?.slug)
    .slice(0, 3);
} catch (error) {
  console.error('[Homepage] Error fetching blog articles:', error);
}

// PERFORMANCE OPTIMIZATION: Parallel fetches with timeout and caching
// Move expensive API calls to client-side or use static data
try {
  // Use Promise.allSettled to fetch in parallel with timeouts
  const fetchWithTimeout = (promise: Promise<any>, ms: number) => {
    return Promise.race([
      promise,
      new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), ms))
    ]);
  };

  const [hotelsResult, skiResult] = await Promise.allSettled([
    // Fetch basic hotels list (no rates) - fast
    fetchWithTimeout(
      searchHotels({
        cityName: 'Telluride',
        countryCode: 'US',
        limit: 30, // Further reduced for speed
      }),
      3000 // 3 second timeout
    ),
    // Fetch ski conditions in parallel
    fetchWithTimeout(getTellurideSkiConditions(), 2000)
  ]);

  // Process hotels result
  if (hotelsResult.status === 'fulfilled') {
    allHotels = hotelsResult.value.data || [];
    
    // Get top 20 hotels using balanced algorithm to ensure we have enough with availability
    // Score = review_score × log(review_count) × amenity_factor
    // We fetch more than needed because not all will have availability
    const candidateHotels = getTopHotels(allHotels, 20);
    
    // Set check-in date (7 days from now) and check-out (7 days later = 14 days from now)
    const now = new Date();
    featuredCheckInDate = format(addDays(now, 7), 'yyyy-MM-dd');
    const featuredCheckOutDate = format(addDays(now, 14), 'yyyy-MM-dd');
    
    // Fetch min rates for candidate hotels to check availability
    // This ensures we can show the best 6 hotels that actually have rooms available
    const candidateHotelIds = candidateHotels.map(h => h.hotel_id);
    if (candidateHotelIds.length > 0) {
      try {
        const minRatesData = await getMinRates({
          hotelIds: candidateHotelIds,
          checkIn: featuredCheckInDate,
          checkOut: featuredCheckOutDate,
          adults: 2,
          currency: 'USD',
          guestNationality: 'US',
          timeout: 10, // Increased timeout for more hotels
        });
        
        // Calculate nights for per-night pricing
        const nights = Math.ceil((new Date(featuredCheckOutDate).getTime() - new Date(featuredCheckInDate).getTime()) / (1000 * 60 * 60 * 24));
        
        // Filter hotels to only those with availability and extract prices
        const hotelsWithAvailability = candidateHotels.filter(hotel => {
          const rateData = minRatesData[hotel.hotel_id];
          if (rateData && rateData.price) {
            // Use price (retail rate with commission already included)
            // MinRates returns TOTAL price, need to divide by nights
            const totalPrice = rateData.price || 0;
            minPrices[hotel.hotel_id] = nights > 0 ? totalPrice / nights : totalPrice;
            return true;
          }
          return false;
        });
        
        // Take the best 6 hotels that have availability
        featuredHotelsData = hotelsWithAvailability.slice(0, 6);
        // Also store top 10 for other uses on the page
        topHotelsData = hotelsWithAvailability.slice(0, 10);
        
        console.log('[Homepage] Loaded hotels with availability:', {
          total: allHotels.length,
          candidates: candidateHotels.length,
          withAvailability: hotelsWithAvailability.length,
          featured: featuredHotelsData.length,
          topRated: topHotelsData.length,
          pricesLoaded: Object.keys(minPrices).length,
          availabilityRate: `${Math.round((hotelsWithAvailability.length / candidateHotels.length) * 100)}%`,
          samplePrices: Object.entries(minPrices).slice(0, 3).map(([id, price]) => ({
            hotelId: id,
            pricePerNight: price,
            nights: nights,
          })),
        });
      } catch (error) {
        console.error('[Homepage] CRITICAL: Error fetching min rates:', error);
        // In production, this should not happen - indicates API or network issue
        // Re-throw to surface the error rather than silently failing
        throw new Error(`Failed to fetch hotel rates: ${error instanceof Error ? error.message : 'Unknown error'}`);
      }
    }
    
    console.log('[Homepage] Loaded hotels (smart algorithm):', {
      total: allHotels.length,
      featured: featuredHotelsData.length,
      topRated: topHotelsData.length
    });
  }

  // Process ski conditions result
  if (skiResult.status === 'fulfilled') {
    skiConditions = skiResult.value;
    console.log('[Homepage] Loaded ski conditions');
  }
} catch (error) {
  console.error('[Homepage] Error in parallel fetch:', error);
}
---

<PageLayout
  title={`Best Places to Stay in Telluride, Colorado | Compare & Book Direct`}
  description={SITE_DESCRIPTION}
  image={heroImage?.url}
>
  <!-- Split Hero Section -->
  <div class="bg-neutral-50 py-16 lg:py-20">
    <Container>
      <SplitHero client:idle heroImage={heroImage} />
    </Container>
  </div>

  <!-- Trust Strip / Feature Boxes -->
  <div class="bg-[#F8F6F3] py-12 lg:py-16">
    <Container>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 max-w-6xl mx-auto">
        <div class="flex flex-col items-center text-center gap-4 group p-8 rounded-xl hover:bg-white/50 transition-all duration-200">
          <div class="w-16 h-16 rounded-2xl flex items-center justify-center flex-shrink-0 group-hover:scale-105 transition-all duration-300" style="background: linear-gradient(135deg, #2D5F4F 0%, #8BA888 100%);">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
            </svg>
          </div>
          <div>
            <h3 class="text-base font-semibold text-[#2C2C2C] mb-1">Secure Booking</h3>
            <p class="text-sm text-[#666]">SSL Encrypted</p>
          </div>
        </div>
        <div class="flex flex-col items-center text-center gap-4 group p-8 rounded-xl hover:bg-white/50 transition-all duration-200">
          <div class="w-16 h-16 rounded-2xl flex items-center justify-center flex-shrink-0 group-hover:scale-105 transition-all duration-300" style="background: linear-gradient(135deg, #2D5F4F 0%, #8BA888 100%);">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <div>
            <h3 class="text-base font-semibold text-[#2C2C2C] mb-1">Instant Confirmation</h3>
            <p class="text-sm text-[#666]">Immediate booking</p>
          </div>
        </div>
        <div class="flex flex-col items-center text-center gap-4 group p-8 rounded-xl hover:bg-white/50 transition-all duration-200">
          <div class="w-16 h-16 rounded-2xl flex items-center justify-center flex-shrink-0 group-hover:scale-105 transition-all duration-300" style="background: linear-gradient(135deg, #2D5F4F 0%, #8BA888 100%);">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M18.364 5.636l-3.536 3.536m0 5.656l3.536 3.536M9.172 9.172L5.636 5.636m3.536 9.192l-3.536 3.536M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-5 0a4 4 0 11-8 0 4 4 0 018 0z" />
            </svg>
          </div>
          <div>
            <h3 class="text-base font-semibold text-[#2C2C2C] mb-1">24/7 Support</h3>
            <p class="text-sm text-[#666]">Always available</p>
          </div>
        </div>
        <div class="flex flex-col items-center text-center gap-4 group p-8 rounded-xl hover:bg-white/50 transition-all duration-200">
          <div class="w-16 h-16 rounded-2xl flex items-center justify-center flex-shrink-0 group-hover:scale-105 transition-all duration-300" style="background: linear-gradient(135deg, #C87859 0%, #D4941D 100%);">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
            </svg>
          </div>
          <div>
            <h3 class="text-base font-semibold text-[#2C2C2C] mb-1">Local Partnerships</h3>
            <p class="text-sm text-[#666]">Direct relationships with Telluride resorts</p>
          </div>
        </div>
      </div>
    </Container>
  </div>

  <!-- Top 10 Hotels Section -->
  {topHotelsData.length > 0 && (
    <div class="bg-white py-16 lg:py-24">
      <Container>
        <div class="max-w-7xl mx-auto">
          <div class="text-center mb-12">
            <div class="inline-flex items-center gap-2 bg-primary-100 text-primary-700 px-4 py-2 rounded-full text-sm font-semibold mb-4">
              <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
              </svg>
              Top Rated Properties
            </div>
            <h2 class="text-4xl lg:text-5xl mb-6 text-neutral-900 font-extrabold" style="font-family: 'Playfair Display', serif;">Best Places to Stay in Telluride</h2>
            <p class="text-lg lg:text-xl text-neutral-600 max-w-3xl mx-auto leading-relaxed">
              Handpicked hotels with the best balance of guest reviews, amenities, and value. These properties consistently deliver exceptional ski vacation experiences.
            </p>
          </div>

          <!-- Top Hotels Grid -->
          <div class="mb-10">
            <FeaturedHotels 
              client:visible
              initialHotels={featuredHotelsData} 
              minPrices={minPrices}
              currency="USD"
              checkInDate={featuredCheckInDate}
              checkOutDate={featuredCheckOutDate}
              adults={2}
            />
          </div>

          <!-- CTA Buttons -->
          <div class="flex flex-wrap justify-center gap-4">
            <a 
              href="/places-to-stay" 
              class="inline-flex items-center gap-2 bg-primary-600 text-white px-8 py-4 rounded-xl font-bold text-lg shadow-card hover:shadow-card-hover hover:bg-primary-700 transition-all duration-300"
            >
              View All Hotels
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </a>
            <a 
              href={`/places-to-stay?location=Telluride&checkIn=${format(addDays(new Date(), 7), 'yyyy-MM-dd')}&checkOut=${format(addDays(new Date(), 14), 'yyyy-MM-dd')}&adults=2`}
              class="inline-flex items-center gap-2 bg-white text-primary-600 border-2 border-primary-600 px-8 py-4 rounded-xl font-bold text-lg shadow-sm hover:shadow-md hover:bg-primary-50 transition-all duration-300"
            >
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
              </svg>
              Search by Dates
            </a>
          </div>
        </div>
      </Container>
    </div>
  )}

  <!-- Featured Blog Section -->
  <FeaturedBlogSection 
    featuredArticle={featuredBlogArticle}
    recentArticles={recentBlogArticles}
  />

  <!-- Ski Conditions & Weather Section -->
  <div class="bg-gradient-to-b from-white via-neutral-50 to-white py-20 lg:py-28">
    <Container>
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-display-sm lg:text-display-md mb-6 text-neutral-900 font-extrabold">Current Mountain Conditions</h2>
          <p class="text-lg lg:text-xl text-neutral-600 max-w-2xl mx-auto leading-relaxed">
            Live ski conditions, weather forecast, and everything you need to plan your perfect ski day
          </p>
        </div>
        
        <!-- Ski Conditions Widget -->
        <div class="mb-12">
          <SkiConditionsWidget client:visible initialConditions={skiConditions} />
        </div>

        <!-- Two Column Layout: Weather + Webcams -->
        <div class="grid lg:grid-cols-3 gap-8 mb-12 items-stretch">
          <!-- Weather Forecast (takes 2 columns) -->
          <div class="lg:col-span-2 flex">
            <CurrentConditions client:visible />
          </div>

          <!-- Webcam Links (takes 1 column) -->
          <div class="lg:col-span-1 flex">
            <WebcamLinks client:visible />
          </div>
        </div>
      </div>
    </Container>
    </div>

  <Container class="py-16 lg:py-20">

    <!-- Things to Do Section -->
    <section class="mb-20">
      <div class="text-center mb-12">
        <h2 class="text-display-sm lg:text-display-md mb-6 text-neutral-900 font-extrabold">Things to Do in Telluride</h2>
        <p class="text-lg lg:text-xl text-neutral-600 max-w-2xl mx-auto leading-relaxed">
          Explore unforgettable activities, tours, and experiences. From thrilling adventures to cultural tours.
        </p>
      </div>
      <FeaturedActivities client:visible limit={6} />
      <div class="text-center mt-16">
        <a 
          href="/things-to-do" 
          class="inline-flex items-center gap-2 bg-primary-600 text-white px-12 py-5 rounded-xl font-bold text-lg shadow-card hover:shadow-card-hover hover:bg-primary-700 hover:scale-105 transition-all duration-300 min-h-[60px]"
        >
          View All Activities
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </a>
      </div>
    </section>

    <!-- Local Information Widgets -->
    <section class="py-20 lg:py-20">
      <Container>
        <div class="max-w-[1400px] mx-auto">
          <div class="text-center mb-12">
            <h2 class="text-display-sm lg:text-display-md mb-6 text-neutral-900 font-extrabold">Plan Your Visit</h2>
            <p class="text-lg lg:text-xl text-neutral-600 max-w-2xl mx-auto leading-relaxed">
              Everything you need to know about visiting Telluride
            </p>
          </div>
          <div class="grid grid-cols-1 lg:grid-cols-3 gap-4 md:gap-6 lg:gap-8" style="align-items: stretch;">
            <GondolaWidget client:visible />
            <EventsWidget client:visible />
            <AirQualityWidget client:visible />
          </div>
        </div>
      </Container>
    </section>

    <!-- Why Choose Us Section -->
    <section class="mb-20">
      <div class="text-center mb-12">
        <h2 class="text-display-sm lg:text-display-md mb-6 text-neutral-900 font-extrabold">Why Book With Us</h2>
        <p class="text-lg lg:text-xl text-neutral-600 max-w-2xl mx-auto leading-relaxed">
          Everything you need for the perfect ski vacation in one place
        </p>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 lg:gap-12">
        <div class="group text-center p-8 rounded-2xl bg-white border border-neutral-200 shadow-card hover:shadow-card-hover transition-all duration-300 hover:-translate-y-1">
          <div class="inline-flex items-center justify-center w-20 h-20 rounded-2xl bg-primary-600 shadow-card group-hover:shadow-card-hover group-hover:scale-110 transition-all duration-300 mb-6">
            <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-4 text-neutral-900">Real-Time Availability</h3>
          <p class="text-neutral-600 leading-relaxed text-lg">
            See current availability and prices updated in real-time from our verified lodging partners across Telluride. We work directly with local resorts and properties to bring you competitive rates comparable to industry leaders.
          </p>
        </div>
        <div class="group text-center p-8 rounded-2xl bg-white border border-neutral-200 shadow-card hover:shadow-card-hover transition-all duration-300 hover:-translate-y-1">
          <div class="inline-flex items-center justify-center w-20 h-20 rounded-2xl bg-primary-600 shadow-card group-hover:shadow-card-hover group-hover:scale-110 transition-all duration-300 mb-6">
            <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z" />
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-4 text-neutral-900">Competitive Pricing</h3>
          <p class="text-neutral-600 leading-relaxed text-lg">
            We partner with local resorts and properties to provide you the best prices, comparable to that of industry leaders. No hidden fees or surprise charges.
          </p>
        </div>
        <div class="group text-center p-8 rounded-2xl bg-white border border-neutral-200 shadow-card hover:shadow-card-hover transition-all duration-300 hover:-translate-y-1">
          <div class="inline-flex items-center justify-center w-20 h-20 rounded-2xl bg-primary-600 shadow-card group-hover:shadow-card-hover group-hover:scale-110 transition-all duration-300 mb-6">
            <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-4 text-neutral-900">Instant Confirmation</h3>
          <p class="text-neutral-600 leading-relaxed text-lg">
            Get instant booking confirmation with secure payment processing and 24/7 customer support
          </p>
        </div>
      </div>
    </section>


    <!-- Final CTA Section - Split Card Design -->
    <section class="bg-white rounded-3xl shadow-2xl overflow-hidden">
      <div class="grid lg:grid-cols-2 gap-0">
        <!-- Left Side - Image -->
        <div class="relative h-[400px] lg:h-auto">
          {ctaImage ? (
            <>
              <img 
                src={optimizePexelsImage(ctaImage.url, { width: 800, quality: 80 })}
                srcset={generateSrcSet(ctaImage.url, [400, 600, 800])}
                sizes="(max-width: 1024px) 100vw, 50vw"
                alt="Telluride Ski Resort" 
                class="w-full h-full object-cover"
                loading="lazy"
                decoding="async"
              />
            </>
          ) : (
            <div class="w-full h-full bg-primary-600"></div>
          )}
          <div class="absolute inset-0 bg-black/30"></div>
          
          <!-- Featured Badge Overlay on Image -->}
          <div class="absolute top-6 left-6 bg-white/95 backdrop-blur-sm rounded-xl px-4 py-2 shadow-lg">
            <div class="flex items-center gap-2">
              <svg class="w-5 h-5 text-primary-600" fill="currentColor" viewBox="0 0 20 20">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
              </svg>
              <span class="font-bold text-sm text-neutral-900">Top-Rated Destination</span>
            </div>
          </div>
        </div>

        <!-- Right Side - Content -->
        <div class="bg-primary-600 p-10 lg:p-14 flex flex-col justify-center text-white relative overflow-hidden">
          {/* Decorative Pattern */}
          <div class="absolute inset-0 opacity-10">
            <div class="absolute inset-0 bg-[radial-gradient(circle_at_top_right,_rgba(255,255,255,0.3),transparent_70%)]"></div>
          </div>

      <div class="relative z-10">
            {/* Header */}
            <div class="mb-8">
              <h2 class="text-3xl md:text-4xl lg:text-5xl mb-4 font-extrabold leading-tight">
                Ready to Plan Your Ski Adventure?
              </h2>
              <p class="text-lg lg:text-xl text-white/95 leading-relaxed font-medium">
          Start exploring Telluride's best places to stay and find your perfect mountain getaway today
        </p>
            </div>

            {/* Key Benefits */}
            <div class="mb-8 space-y-3">
              <div class="flex items-center gap-3">
                <div class="w-6 h-6 rounded-full bg-white/20 flex items-center justify-center flex-shrink-0">
                  <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                </div>
                <span class="text-white/95 font-medium">Curated selection of top-rated hotels</span>
              </div>
              <div class="flex items-center gap-3">
                <div class="w-6 h-6 rounded-full bg-white/20 flex items-center justify-center flex-shrink-0">
                  <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                </div>
                <span class="text-white/95 font-medium">Competitive rates through our relationships with local resorts</span>
              </div>
              <div class="flex items-center gap-3">
                <div class="w-6 h-6 rounded-full bg-white/20 flex items-center justify-center flex-shrink-0">
                  <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                  </svg>
                </div>
                <span class="text-white/95 font-medium">Detailed trail maps and ski conditions</span>
              </div>
            </div>

            {/* CTA Buttons */}
            <div class="flex flex-col sm:flex-row gap-4">
        <a 
          href="/places-to-stay" 
                class="inline-flex items-center justify-center gap-3 bg-white text-primary-600 px-8 py-4 rounded-xl font-bold text-lg shadow-xl hover:shadow-2xl hover:scale-105 transition-all duration-300"
              >
                Browse All Hotels
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                </svg>
              </a>
              <a 
                href="/trail-map" 
                class="inline-flex items-center justify-center gap-3 bg-white/10 backdrop-blur-sm text-white border-2 border-white/30 px-8 py-4 rounded-xl font-bold text-lg hover:bg-white/20 hover:border-white/50 transition-all duration-300"
              >
                View Trail Map
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l4.553 2.276A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7" />
          </svg>
        </a>
            </div>
          </div>
        </div>
      </div>
    </section>
  </Container>
</PageLayout>
